wifi_ssid: "YOUR_2.4G_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
wifi_fallback_password: "CHANGE_ME_123"
# Stage 2 省电版可选：给设备固定 DHCP 租约后填写，取消 YAML 中 manual_ip 注释可减少联网等待。
wifi_static_ip: "10.30.5.173"
wifi_gateway: "10.30.5.1"
wifi_subnet: "255.255.255.0"
# 可用 `openssl rand -base64 32` 生成
esphome_api_encryption_key: "REPLACE_WITH_32_BYTE_BASE64_KEY"
# 可用 `openssl rand -hex 16` 生成
esphome_ota_password: "REPLACE_WITH_OTA_PASSWORD"

# Stage 2 API 直连时再补这一项；Stage 1 首刷不需要。
infohub_eink_device_url: "http://10.30.5.172:8080/dashboard/eink/device.json?token=REPLACE_WITH_INFOHUB_DASHBOARD_TOKEN&refresh=300"
